<div class="basic-chat-card">
    <header>
        <img src="{{item.img}}"/>
        <h2>{{item.name}}</h2>
    </header>
    <div class="card-content">
        {{{enrichedDescription}}}
    </div>
</div>
